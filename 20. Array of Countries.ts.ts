// define an array of countries
let countries: string[] = [`USA`, `Canada`, `Australia`, `Japan`, `Germany`, `Brazil`];
// print the array elements
console.log(`list of countries`);
for(let country of countries){
    console.log(country);
}